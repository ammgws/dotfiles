#!/usr/bin/fish

# Simple simulation of nvidia-smi for testing during i3status-rs development

echo "GTX1080, 4196, "(random 1024 3999)

#set codes 0 9
set codes 0
set count (count $codes)
set i (math --scale=0 (random)"%"(count $codes)"+1")
exit $codes[$i]
